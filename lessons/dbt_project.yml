name: 'lessons'
version: '1.0.0'

profile: 'lessons'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:      
  - "target"

models:
  lessons:
    staging:
      +materialized: table
    # intermediate:
    #   +materialized: table
    # marts:
    #   +materialized: table
